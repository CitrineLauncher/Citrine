<?xml version="1.0" encoding="utf-8"?>
<local:WindowEx
    x:Class="Citrine.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Citrine"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Citrine">

    <Grid AllowDrop="True" DragEnter="ContentRoot_DragEnter">
        <Grid.RowDefinitions>
            <RowDefinition Height="38" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="TitleBar" Title="Citrine" Grid.Row="0" Canvas.ZIndex="1" IsTabStop="False" />
        <ListView Grid.Row="1" Margin="20,0" MaxWidth="720" VerticalAlignment="Stretch" Canvas.ZIndex="1"
            ItemsSource="{x:Bind ViewModel.Notifications}"
            SelectionMode="None">

            <ListView.Resources>
                <XamlUICommand x:Key="CloseNotificationCommand" Command="{x:Bind ViewModel.CloseNotificationCommand}" />
                <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="Margin" Value="0,3" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    <Setter Property="IsTabStop" Value="False" />
                </Style>
            </ListView.Resources>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ToastNotification">
                    <local:ToastNotificationView Notification="{x:Bind}" CloseCommand="{StaticResource CloseNotificationCommand}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    
                </ControlTemplate>
            </ListView.Template>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- Use a non virtualising stack panel  -->
                    <StackPanel  />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerTransitions>
                <AddDeleteThemeTransition />
                <ReorderThemeTransition />
            </ListView.ItemContainerTransitions>
        </ListView>
        <NavigationView x:Name="NavView" Grid.Row="0" Grid.RowSpan="2" Canvas.ZIndex="0"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            PaneDisplayMode="Left"
            OpenPaneLength="192"
            CompactPaneLength="52"
            SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="JavaNavItem" Content="Java" IsEnabled="False">
                    <NavigationViewItem.Icon>
                        <ImageIcon Source="{StaticResource MinecraftJavaIcon}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="BedrockNavItem" Content="Bedrock">
                <NavigationViewItem.Icon>
                        <ImageIcon Source="{StaticResource MinecraftBedrockIcon}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Resources>
                <Thickness x:Key="NavigationViewPaneContentGridMargin">-1,38,-1,3</Thickness>
                <CornerRadius x:Key="NavigationViewContentGridCornerRadius">0</CornerRadius>
                <Thickness x:Key="NavigationViewContentGridBorderThickness">1,0,0,0</Thickness>
                <x:Double x:Key="NavigationViewItemOnLeftIconBoxHeight">24</x:Double>
                <x:Double x:Key="NavigationViewItemOnLeftMinHeight">44</x:Double>
                <Style TargetType="NavigationViewItem">
                    <Setter Property="FontSize" Value="16"/>
                </Style>
            </NavigationView.Resources>
        </NavigationView>
    </Grid>
</local:WindowEx>
