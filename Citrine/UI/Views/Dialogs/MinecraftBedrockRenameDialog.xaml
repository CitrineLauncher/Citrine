<?xml version="1.0" encoding="utf-8"?>
<local:ContentDialogEx
    x:Class="Citrine.MinecraftBedrockRenameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Citrine"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="580" IsTabStop="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ImageIcon Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Width="80" Height="80" Source="{x:Bind GetPackageIconSource(GamePackage), Mode=OneTime}" />
        <local:TextBoxEx x:Name="NameTagText" Grid.Row="0" Grid.Column="2" PrefixText="{x:Bind GamePackage.NormalizedVersion, Mode=OneTime}" Text="{x:Bind GamePackage.NameTag, Mode=OneTime}" SelectionStart="{x:Bind GetNameTagLength(GamePackage), Mode=OneTime}" Height="40" FontSize="21">
            <local:TextBoxEx.Resources>
                <ResourceDictionary>
                    <SolidColorBrush x:Key="TextControlBackground" Color="Transparent" />
                </ResourceDictionary>
            </local:TextBoxEx.Resources>
        </local:TextBoxEx>
        <TextBlock Grid.Row="1" Grid.Column="2" Text="{x:Bind GetPackageDescriptionString(GamePackage), Mode=OneTime}" Margin="2,4,0,0" FontSize="17" Foreground="{ThemeResource TextFillColorSecondary}" />
    </Grid>
</local:ContentDialogEx>
