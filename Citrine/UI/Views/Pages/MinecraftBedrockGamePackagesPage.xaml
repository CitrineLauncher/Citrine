<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Citrine.MinecraftBedrockGamePackagesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Citrine"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Page.Resources>
        <XamlUICommand x:Name="InvokeGamePackageActionCommand" ExecuteRequested="InvokeGamePackageActionCommand_ExecuteRequested" />
    </Page.Resources>
    <Grid MaxWidth="720" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Image Source="{StaticResource MinecraftBedrockLogo}" />
        <TextBox x:Uid="MinecraftBedrock_GamePackages_Search" Text="{x:Bind ViewModel.FilteredGamePackages.Filter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Margin="0,8,0,4" FontSize="17" Height="36" IsSpellCheckEnabled="False" />
        <ScrollViewer Grid.Row="2" Visibility="{x:Bind local:Convert.BoolToInverseVisibility(ViewModel.FilteredGamePackages.IsEmpty), Mode=OneWay}">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.FilteredGamePackages}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:MinecraftBedrockGamePackageItem">
                        <local:MinecraftBedrockGamePackageView Margin="0,1.5" GamePackage="{x:Bind}" InvokeActionCommand="{StaticResource InvokeGamePackageActionCommand}" />
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
        <Button x:Name="ImportButton" x:Uid="MinecraftBedrock_GamePackages_Import" x:Load="{x:Bind ViewModel.SupportsImporting}" IsEnabled="{x:Bind ViewModel.CanStartImporting}" Grid.Row="3" HorizontalAlignment="Right" Margin="0,6,0,0" Click="ImportButton_Click" />
    </Grid>
</Page>
