<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Citrine.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Citrine"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <ScrollViewer MaxWidth="800" VerticalAlignment="Center">
        <StackPanel>

            <TextBlock x:Uid="About_About" FontSize="16" FontWeight="Medium" />

            <Grid Height="106" Margin="0,6,0,0" Padding="16,20" ColumnSpacing="16" Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="4" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="26" />
                    <RowDefinition Height="38" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ImageIcon Width="64" Height="64" VerticalAlignment="Center" Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Source="{ThemeResource CitrineIcon}" />
                <TextBlock Text="Citrine" FontSize="20" Grid.Row="0" Grid.Column="1"/>
                <TextBlock Text="{x:Bind ViewModel.VersionInfoString}" FontSize="16" Grid.Row="1" Grid.Column="1" Foreground="{ThemeResource TextFillColorSecondary}" />
            </Grid>

            <Button Height="70" Margin="0,3,0,0" Padding="16" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" HorizontalContentAlignment="Stretch" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    Command="{x:Bind ViewModel.OpenHyperlinkCommand}"
                    CommandParameter="https://github.com/CitrineLauncher/Citrine">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ImageIcon Margin="2,0" Source="{ThemeResource GitHubIcon}" Grid.Column="0" Width="24" Height="24" />
                    <TextBlock x:Uid="About_GitHubRepository" VerticalAlignment="Center" FontSize="16" Grid.Column="1" />
                    <FontIcon Glyph="&#xE8A7;" VerticalAlignment="Center" Grid.Column="2" HorizontalAlignment="Right" FontSize="14" />
                </Grid>
            </Button>

            <InfoBar Margin="0,3,0,0" IsOpen="True" IsClosable="False" Title="Not affiliated with or endorsed by Mojang / Microsoft." />

            <TextBlock Visibility="Collapsed"
                 x:Uid="About_Runtime" Margin="0,24,0,0" FontSize="16" FontWeight="Medium" />

            <Grid Visibility="Collapsed" 
                  Height="70" Margin="0,6,0,2" Padding="16" ColumnSpacing="16" Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="4" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <FontIcon Glyph="&#xE9F5;" Margin="2,0" VerticalAlignment="Center" Grid.Column="0" FontSize="24" />
                <TextBlock Text="{x:Bind ViewModel.RuntimeInfoString}" VerticalAlignment="Center" FontSize="16" Grid.Column="1" />
            </Grid>
            
        </StackPanel>
    </ScrollViewer>
</Page>
