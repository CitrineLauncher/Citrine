<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Citrine.MinecraftJavaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Citrine"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Background="{ThemeResource ControlFillColorDefaultBrush}"
              CornerRadius="4"
              BorderThickness="1"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <NavigationView
                Grid.Column="0"
                IsSettingsVisible="False"
                IsBackButtonVisible="Collapsed"
                HorizontalAlignment="Left"
                VerticalAlignment="Center" 
                PaneDisplayMode="Top">

                <NavigationView.MenuItems>
                    <NavigationViewItem x:Uid="MinecraftJava_Play" />
                    <NavigationViewItem x:Uid="MinecraftJava_Installations" />
                </NavigationView.MenuItems>

                <NavigationView.Resources>
                    <x:Double x:Key="NavigationViewTopPaneHeight">52</x:Double>
                    <x:Double x:Key="TopNavigationViewPaneCustomContentMinWidth">0</x:Double>
                    <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                    <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                    <Style TargetType="NavigationViewItem">
                        <Setter Property="FontSize" Value="18"/>
                    </Style>
                </NavigationView.Resources>
            </NavigationView>
        </Grid>
        <local:FrameEx x:Name="Frame" Grid.Row="1" />
    </Grid>
</Page>
